第一步
进入你的剧本所在目录（如果不是默认的）
cd C:\Users\yuyuyuuy
先输入此代码找到文件所在地，
node bot.mjs
在运行此代码命令运行，在git工具里面运行，
共两个步骤运行夹子机器人，
3.如何关闭，

如何停止运行？
快捷键终止：
在 Git Bash 终端内按Ctrl + C（Windows/Linux 通用）
这样就可以立刻停止bot.js运行。

关闭 Git 终端：
直接关闭 Git Bash 窗口，也强制终止运行中的bot.js。

任务管理器终止（如果Git Bash关闭不了）：

点击Ctrl + Shift + Esc打开该项目
找到node.exe进程
右键→选择结束任务，脚本就会被终止。

最后主要点，所有机器人运行必须在docker Desktop镜像容器内运行，1，每次运行必须先打开docker Desktop软件，在继续，如果需要拉取镜像，必须是在文件夹里面拉取，而不是在js文件里面拉取，
